// Imports
#include "structs/Human.h"

int main(int argc, char const *argv[])
{
    initializeData();
    Human * humanity[2];
    for (int i = 0; i < 2; i++)
        humanity[i] = NULL; 
    
    return 0;
}
